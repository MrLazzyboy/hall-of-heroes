# 📌 API Документация

## 🔹 Аутентификация (`/auth`)

### **1. Регистрация**
```http
POST /auth/register
```

#### 🔹 **Описание**
Регистрирует нового пользователя.
#### 🔹 **Тело запроса (JSON)**
```json
{
  "username": "string", // Логин (уникальный)
  "email": "string", // Email (уникальный)
  "phone": "string", // Телефон (уникальный)
  "password": "string", // Пароль
  "name": "string", // (необязательно) Имя
  "bio": "string", // (необязательно) Описание профиля
  "socialLink": "string" // (необязательно) Ссылка на соцсеть
}
```
#### 🔹 **Ответ**
```json
{
  "message": "Пользователь зарегистрирован",
  "userId": "string",
  "token": "string"
}
```

### **2. Авторизация**
```http
POST /auth/login
```
#### 🔹 **Описание**
Авторизует пользователя.
#### 🔹 **Тело запроса (JSON)**
```json
{
  "email": "string",
  "password": "string"
}
```
#### 🔹 **Ответ**
```json
{
  "message": "Успешная авторизация",
  "token": "string"
}
```

### **3. Выход из системы**
```http
DELETE /auth/session
```
#### 🔹 **Описание**
Разлогинивает пользователя (фронт удаляет токен).
#### 🔹 **Ответ**
```json
{
  "message": "Логаут выполнен"
}
```

### **4. Проверка сессии**
```http
GET /auth/session
```
#### 🔹 **Описание**
Проверяет, действителен ли токен.
#### 🔹 **Ответ**
```json
{
  "message": "Сессия действительна",
  "userId": "string"
}
```

---

## 🔹 Пользователи (`/users`)

### **5. Получение информации о себе**
```http
GET /users/me
```
#### 🔹 **Описание**
Возвращает данные авторизованного пользователя.
#### 🔹 **Ответ**
```json
{
  "username": "string",
  "email": "string",
  "profile": {
    "avatarUrl": "string",
    "bio": "string"
  }
}
```

### **6. Обновление информации о себе**
```http
PATCH /users/me
```
#### 🔹 **Описание**
Обновляет профиль пользователя.
#### 🔹 **Тело запроса (JSON)**
```json
{
  "username": "string",
  "profile": {
    "bio": "string",
    "avatarUrl": "string"
  }
}
```

### **7. Удаление аккаунта**
```http
DELETE /users/me
```
#### 🔹 **Описание**
Удаляет учетную запись пользователя.

### **8. Получение другого пользователя по ID**
```http
GET /users/:id
```
#### 🔹 **Описание**
Возвращает публичные данные о пользователе.

### **9. Загрузка аватара**
```http
POST /users/me/avatar
```
#### 🔹 **Описание**
Загружает аватар пользователя.

---

## 🔹 События (`/events`)

### **10. Создание события**
```http
POST /events
```
#### 🔹 **Описание**
Создает новое событие (только для авторизованных пользователей).
#### 🔹 **Тело запроса (JSON)**
```json
{
  "title": "string",
  "description": "string",
  "date": "YYYY-MM-DD",
  "invitations": ["userId1", "userId2"]
}
```

### **11. Получение всех событий**
```http
GET /events
```
#### 🔹 **Описание**
Возвращает список всех событий.

### **12. Получение события по ID**
```http
GET /events/:id
```
#### 🔹 **Описание**
Возвращает информацию о конкретном событии.

### **13. Редактирование события**
```http
PUT /events/:id
```
#### 🔹 **Описание**
Редактирует событие (только автор события).

### **14. Удаление события**
```http
DELETE /events/:id
```
#### 🔹 **Описание**
Удаляет событие (только автор).

---

## 🔹 Админские API (`/admin`)

### **15. Подтверждение события**
```http
POST /admin/events/:id/approve
```
#### 🔹 **Описание**
Админ утверждает событие.

### **16. Отклонение события**
```http
POST /admin/events/:id/reject
```
#### 🔹 **Описание**
Админ отклоняет событие с причиной.

### **17. Блокировка пользователя**
```http
PATCH /admin/users/:id/block
```
#### 🔹 **Описание**
Админ блокирует пользователя.

### **18. Разблокировка пользователя**
```http
PATCH /admin/users/:id/unblock
```
#### 🔹 **Описание**
Админ разблокирует пользователя.

### **19. Добавление фильтра**
```http
POST /admin/filters
```
#### 🔹 **Описание**
Добавляет новый фильтр.

### **20. Управление новостями**
- **Создание**:
  ```http
  POST /admin/news
  ```
- **Обновление**:
  ```http
  PUT /admin/news/:id
  ```
- **Удаление**:
  ```http
  DELETE /admin/news/:id
  ```

---
